name: sync-tabler

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  tabler-check:
    runs-on: ubuntu-latest
    outputs:
      new: ${{ steps.check.outputs.new }}
      tag: ${{ steps.release.outputs.tag }}
    steps:
      - uses: actions/checkout@v6

      - name: Get latest release tag
        id: release
        run: |
          TAG=$(curl -s https://api.github.com/repos/tabler/tabler-icons/releases/latest | jq -r '.tag_name')
          echo "tag=$TAG" >> $GITHUB_OUTPUT

      - name: Check if new release
        id: check
        run: |
          CURRENT=$(jq -r ".tabler" versions.json)
          LATEST=${{ steps.release.outputs.tag }}
          CLEAN=$(echo $LATEST | sed 's/[^0-9.]//g')
          if [[ "$CURRENT" != "$CLEAN" ]]; then
            echo "new=true" >> $GITHUB_OUTPUT
          else
            echo "new=false" >> $GITHUB_OUTPUT
          fi

  tabler-sync:
    runs-on: ubuntu-latest
    needs: tabler-check
    if: ${{ needs.tabler-check.outputs.new == 'true' }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 1

      - uses: actions/setup-go@v6
        with:
          go-version-file: ./go.mod

      - name: Sparse clone tabler icons
        run: |
          git clone --depth 1 --branch ${{ needs.tabler-check.outputs.tag }} https://github.com/tabler/tabler-icons tabler-repo
          cd tabler-repo
          git sparse-checkout init --cone
          git sparse-checkout set icons

      - name: Process new tabler icons
        id: sync
        run: |
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git config user.name "github-actions[bot]"
          go install github.com/a-h/templ/cmd/templ@latest
          go run ./cmd/sync/main.go tabler
          rm -rf ./tabler-repo
          templ fmt tabler
          templ generate
          git add .
          TAG=$(curl -s https://api.github.com/repos/tabler/tabler-icons/releases/latest | jq -r '.tag_name')
          CLEAN_TAG=$(echo $TAG | sed 's/[^0-9.]//g')
          CLEAN_TAG=$(echo "${{ needs.tabler-check.outputs.tag }}" | sed 's/[^0-9.]//g')
          jq --arg v "$CLEAN_TAG" '.tabler = $v' versions.json > tmp.json && mv tmp.json versions.json
          if [ -n "$(git status --porcelain)" ]; then
            git commit -m "ci: sync with tabler icons repo"
            echo "changes=true" >> $GITHUB_OUTPUT
          else
            echo "changes=false" >> $GITHUB_OUTPUT
          fi

      - name: Submit PR for new tabler icons
        if: ${{ steps.sync.outputs.changes == 'true' }}
        uses: peter-evans/create-pull-request@v8
        with:
          title: "[ci] tabler icons sync"
          labels: ci,sync
          branch: sync
          assignees: dimmerz92
