name: sync-ionic-icons

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  ionic-check:
    runs-on: ubuntu-latest
    outputs:
      new: ${{ steps.check.outputs.new }}
      tag: ${{ steps.release.outputs.tag }}
    steps:
      - uses: actions/checkout@v6

      - name: Get latest release tag
        id: release
        run: |
          TAG=$(curl -s https://api.github.com/repos/ionic-team/ionicons/releases/latest | jq -r '.tag_name')
          echo "tag=$TAG" >> $GITHUB_OUTPUT

      - name: Check if new release
        id: check
        run: |
          CURRENT=$(jq -r ".ionicons" versions.json)
          LATEST=${{ steps.release.outputs.tag }}
          CLEAN=$(echo $LATEST | sed 's/[^0-9.]//g')
          if [[ "$CURRENT" != "$CLEAN" ]]; then
            echo "new=true" >> $GITHUB_OUTPUT
          else
            echo "new=false" >> $GITHUB_OUTPUT
          fi

  ionic-sync:
    runs-on: ubuntu-latest
    needs: ionic-check
    if: ${{ needs.ionic-check.outputs.new == 'true' }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 1

      - uses: actions/setup-go@v6
        with:
          go-version-file: ./go.mod

      - name: Sparse clone ionic icons
        run: |
          git clone --depth 1 --branch ${{ needs.ionic-check.outputs.tag }} https://github.com/ionic-team/ionicons ionicons-repo
          cd ionicons-repo
          git sparse-checkout init --cone
          git sparse-checkout set src/svg

      - name: Process new ionic icons
        id: sync
        run: |
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git config user.name "github-actions[bot]"
          go install github.com/a-h/templ/cmd/templ@latest
          go run ./cmd/sync/main.go ionicons
          rm -rf ./ionicons-repo
          templ fmt ionicons
          templ generate
          CLEAN_TAG=$(echo "${{ needs.ionic-check.outputs.tag }}" | sed 's/[^0-9.]//g')
          jq --arg v "$CLEAN_TAG" '.ionicons = $v' versions.json > tmp.json && mv tmp.json versions.json
          git add .
          if [ -n "$(git status --porcelain)" ]; then
            git commit -m "ci: sync with ionic icons repo"
            echo "changes=true" >> $GITHUB_OUTPUT
          else
            echo "changes=false" >> $GITHUB_OUTPUT
          fi

      - name: Submit PR for new ionic icons
        if: ${{ steps.sync.outputs.changes == 'true' }}
        uses: peter-evans/create-pull-request@v8
        with:
          title: "[ci] ionic icons sync"
          labels: ci,sync
          branch: sync
          assignees: dimmerz92
